import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import {useAuth} from '../../context/AuthContext.jsx'
export default function Register(){const {signIn}=useAuth(),nav=useNavigate(),[f,setF]=useState({fullName:'',username:'',email:'',password:''}),[error,setError]=useState('');return <div className="auth-wrap"><div className="auth-card"><span className="brand">Inventory</span><h1>Register</h1><p className="muted">Create a requester account.</p>{error&&<div className="flash error">{error}</div>}<form onSubmit={e=>{e.preventDefault();import('../../services/inventoryStore.js').then(({register})=>{const r=register(f.fullName,f.username,f.email,f.password);if(r.error)setError(r.error);else{signIn(f.username,f.password);nav('/portal')}})}}>{[['fullName','Full name'],['username','Username'],['email','Email'],['password','Password']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input type={k==='password'?'password':k==='email'?'email':'text'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/></div>)}<button className="full-width">Create account</button></form><div className="divider"/><Link to="/login">Back to sign in</Link></div></div>}
