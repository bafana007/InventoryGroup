import PageHead from '../../components/PageHead/PageHead.jsx';import StatCard from '../../components/StatCard/StatCard.jsx';import * as Store from '../../services/inventoryStore';
export default function Reports(){const m=Store.metrics(),ms=Store.movements();return <main><PageHead title="Reports" subtitle="Inventory valuation and movement ledger."/><div className="grid cols-4"><StatCard value={m.productCount} label="Products"/><StatCard value={m.totalUnits} label="Units"/><StatCard value={m.stockValue.toFixed(2)} label="Stock value" inverted/><StatCard value={m.lowStockCount} label="Low stock"/></div><div className="card mt-24"><h2>Valuation by product</h2><div className="table-wrap"><table><thead><tr><th>Product</th><th>Qty</th><th>Unit price</th><th>Value</th></tr></thead><tbody>{Store.products().map(p=><tr key={p.id}><td>{p.name}</td><td>{p.quantity}</td><td>{p.unitPrice.toFixed(2)}</td><td>{Store.stockValue(p).toFixed(2)}</td></tr>)}</tbody></table></div></div><div className="card mt-20"><h2>Movement ledger</h2><div className="table-wrap"><table><thead><tr><th>Date</th><th>Item</th><th>Type</th><th>Qty</th><th>User</th><th>Note</th></tr></thead><tbody>{ms.map(mv=>{const p=Store.byId('products',mv.productId),u=Store.byId('users',mv.userId);return <tr key={mv.id}><td>{new Date(mv.createdAt).toLocaleString()}</td><td>{p?.name}</td><td>{mv.type}</td><td>{mv.quantity}</td><td>{u?.username||'system'}</td><td>{mv.note}</td></tr>})}</tbody></table></div></div></main>}
