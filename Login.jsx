import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../../context/AuthContext.jsx'
export default function Login(){const {signIn}=useAuth(),nav=useNavigate(),[form,setForm]=useState({username:'',password:''}),[error,setError]=useState('');return <div className="auth-wrap"><div className="auth-card"><span className="brand">Inventory</span><p className="muted">Sign in to your portal.</p>{error&&<div className="flash error">{error}</div>}<form onSubmit={e=>{e.preventDefault();const r=signIn(form.username,form.password);if(r.error)setError(r.error);else nav(r.user.role==='admin'?'/admin':r.user.role==='employee'?'/employee':'/portal')}}><div className="field"><label>Username</label><input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required autoFocus/></div><div className="field"><label>Password</label><input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></div><button className="full-width">Sign in</button></form><div className="divider"/><p className="muted">No account? <Link to="/register">Register as a requester</Link></p><div className="divider"/><p className="muted">Direct portal access:</p><p><Link className="btn" to="/admin-portal">Admin portal</Link> <Link className="btn" to="/employee/login">Employee portal</Link></p><ul className="demo-list"><li>admin / admin123</li><li>employee / employee123</li><li>user / user123</li></ul></div></div>}
