import * as Store from '../../services/inventoryStore'
export default function UserTable({items=Store.users(),actor,onRole,onToggle}){return <div className="table-wrap"><table><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th><th></th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td><strong>{u.fullName}</strong><div className="muted">@{u.username}</div></td><td>{u.email}</td><td><select className="w-auto" value={u.role} disabled={u.id===actor?.id} onChange={e=>onRole?.(u.id,e.target.value)}><option>admin</option><option>employee</option><option>user</option></select></td><td><span className="badge">{u.isActive?'active':'inactive'}</span></td><td>{new Date(u.createdAt).toLocaleDateString()}</td><td>{u.id!==actor?.id&&<button className="small ghost" onClick={()=>onToggle?.(u.id)}>{u.isActive?'Deactivate':'Activate'}</button>}</td></tr>)}</tbody></table></div>}
