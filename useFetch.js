import {useEffect,useState} from 'react';export default function useFetch(url,options){const [state,setState]=useState({data:null,loading:!!url,error:null});useEffect(()=>{if(!url)return;let live=true;setState({data:null,loading:true,error:null});fetch(url,options).then(r=>{if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}).then(data=>live&&setState({data,loading:false,error:null})).catch(error=>live&&setState({data:null,loading:false,error}));return()=>{live=false}},[url]);return state}
