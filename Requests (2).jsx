import {useState} from 'react';import PageHead from '../../components/PageHead/PageHead.jsx';import RequestTable from '../../components/RequestTable/RequestTable.jsx';import * as Store from '../../services/inventoryStore';import {useAuth} from '../../context/AuthContext.jsx'
export default function Requests(){const {user}=useAuth(),[,rer]=useState(0);return <main><PageHead title="Requests" subtitle="Handle incoming requester orders."/><div className="card"><RequestTable items={Store.requests()} admin onAction={(id,a)=>{const r=Store.handleRequest(id,a,user);if(r.error)alert(r.error);rer(x=>x+1)}}/></div></main>}
