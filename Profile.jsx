import {useState} from 'react';import PageHead from '../../components/PageHead/PageHead.jsx';import * as Store from '../../services/inventoryStore';import {useAuth} from '../../context/AuthContext.jsx'
export default function Profile(){const {user,refresh}=useAuth(),[f,setF]=useState({username:user.username,fullName:user.fullName,email:user.email,currentPassword:'',newPassword:''}),[msg,setMsg]=useState('');return <main><PageHead title="My profile" subtitle={`@${user.username} · ${user.role}`}/><div className="card"><form onSubmit={e=>{e.preventDefault();const r=Store.updateProfile(user,f);setMsg(r.error||'Profile saved.');if(!r.error)refresh()}}>{[['username','Username'],['fullName','Full name'],['email','Email']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input type={k==='email'?'email':'text'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/></div>)}<div className="divider"/><div className="field"><label>Current password</label><input type="password" value={f.currentPassword} onChange={e=>setF({...f,currentPassword:e.target.value})}/></div><div className="field"><label>New password</label><input type="password" minLength="6" value={f.newPassword} onChange={e=>setF({...f,newPassword:e.target.value})}/></div>{msg&&<div className="flash">{msg}</div>}<button>Save changes</button></form></div></main>}
